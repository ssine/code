#include <iostream>
using namespace std;

int maxn = 21;

int m[maxn][maxn], order[maxn];
int n;

int main() {
    cin >> n;
    while(true) {
        for(int i = 0; i < n; i++)
            cin >> order[i];
        
    }

    return 0;
}
