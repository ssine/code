#include <iostream>
#include <cstdio>
#include <string>
using namespace std;

int varv[26];
bool appear[26];

void init() {
    for(int i = 0; i < 26; i++)
}

int main() {
    freopen(".\\in&outputs\\in39", "r", stdin);
    freopen(".\\in&outputs\\out39", "w", stdout);
    string s;
    while(getline(cin, s)) {
        init();

        preparse(s);

        int val = calc
    }
    return 0;
}
